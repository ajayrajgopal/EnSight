<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group
        android:id="@+id/menu_top"
        android:checkableBehavior="single">
        <item
            android:id="@+id/all"
            android:icon="@drawable/ic_menu_all"
            android:title="All News"
            android:checkable="true"/>
    </group>
    <group
        android:id="@+id/menu_mid"
        android:checkableBehavior="single">
        <item
            android:id="@+id/cyber"
            android:checkable="true"
            android:icon="@drawable/ic_menu_cyber"
            android:title="Cyber Attacks" />

    </group>
    <group
        android:id="@+id/menu_bottom"
        android:checkableBehavior="single">
        <item
            android:id="@+id/vulnerabilities"
            android:checkable="true"
            android:icon="@drawable/ic_menu_vulnerabilities"
            android:title="Vulnerabilities"/>
    </group>
    <group
    android:id="@+id/menu_bottom2"
    android:checkableBehavior="single">
        <item
            android:id="@+id/about"
            android:icon="@drawable/ic_menu_about"
            android:title="About Us"/>
    </group>
    <group
        android:id="@+id/menu_bottom3"
        android:checkableBehavior="single">
        <item
            android:id="@+id/feedback"
            android:icon="@drawable/ic_menu_feedback"
            android:title="Feedback"/>
    </group>
</menu>
